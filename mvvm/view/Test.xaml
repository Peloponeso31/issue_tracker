<UserControl x:Class="Comisión_Estatal_de_Búsqueda_del_Estado_de_Veracruz.mvvm.view.Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Comisión_Estatal_de_Búsqueda_del_Estado_de_Veracruz.mvvm.view"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <WrapPanel Orientation="Horizontal"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center" 
                   Grid.Column="1">
            <Button Content="Reporte" Command="{Binding ReportesRelayCommand}"/>
            <Button Content="Otra pestaña" Command="{Binding OtraPestanaRelayCommand}"/>
            <Button Content="Y otra pestaña"/>
            <Button Content="Otra pestaña"/>
            <Button Content="Y otra pestaña"/>
            <Button Content="Otra pestaña"/>
            <Button Content="Y otra pestaña"/>
            <Button Content="Otra pestaña"/>
            <Button Content="Y otra pestaña"/>
            <Button Content="Otra pestaña"/>
            <Button Content="Y otra pestaña"/>
        </WrapPanel>

        <Grid Grid.Row="1" 
              Grid.ColumnSpan="3" 
              Background="Aqua">

            <ContentControl Content="{Binding PestanaActual}"/>

        </Grid>
    </Grid>
</UserControl>
